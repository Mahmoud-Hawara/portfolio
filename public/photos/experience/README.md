# Experience workplace photos

Add images here and reference them in `src/data/portfolio.ts`.

| File | Role |
|------|------|
| `microsoft.png` | Microsoft (added) |
| `noon-1.png` | noon (replace with your photo) |
| `guc-1.png` | German University in Cairo (replace with your photo) |

`noon-1.png` and `guc-1.png` currently use a placeholder copy for preview — swap in your real images when ready.

Use `.png`, `.jpg`, or `.webp`. Update the `src` paths in `portfolio.ts` if you use different names.
